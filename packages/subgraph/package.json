{
  "name": "@web3/subgraph",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/",
    "create:local": "graph create --node http://127.0.0.1:8020/ p2p-escrow",
    "deploy:local": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://localhost:5001 p2p-escrow"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.79.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
